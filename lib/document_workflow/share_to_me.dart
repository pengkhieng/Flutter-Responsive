import 'package:flutter/material.dart';

class ShareToMe extends StatelessWidget {
  const ShareToMe({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue[100],
      body: Center(
        child: Text("SHARE TO ME"),
      ),
    );
  }
}
