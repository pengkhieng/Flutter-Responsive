import 'package:flutter/material.dart';

class Mentioned extends StatelessWidget {
  const Mentioned({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.pink,
      body: Center(
        child: Text("MENTIONED"),
      ),
    );
  }
}
